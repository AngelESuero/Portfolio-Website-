---
import BaseLayout from '../layouts/BaseLayout.astro';
import profile from '../data/profile';
import featuredThreads from '../data/featured_threads.json';
import { getCollection } from 'astro:content';
import UntitledPlayer from '../components/UntitledPlayer.astro';
import EmbedCard from '../components/EmbedCard.astro';
import AIAccountabilityPanel from '../components/AIAccountabilityPanel.astro';
import HomeTabReflections from '../components/HomeTabReflections.astro';
import HomeSocialFeeds from '../components/HomeSocialFeeds.astro';
import { LINK_HUB_CATEGORIES, type AccentColorKey, type LinkHubItem } from '../data/linkhub';
import igManifest from '../data/ig.json';
import xFallbackItems from '../data/x-fallback';
import youtubeFeed from '../data/youtube.json';

import rooms from '../data/rooms';
import agiTimelineData from '../data/agi-timeline.json';

const allProjects = await getCollection('projects');
const allPosts = (await getCollection('posts')).sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
const threads = featuredThreads.featured_threads;
const categoryItems = new Map(LINK_HUB_CATEGORIES.map((category) => [category.id, category.items]));
const pickItems = (categoryId: string, ids: string[]): LinkHubItem[] => {
  const items = categoryItems.get(categoryId) || [];
  return ids
    .map((id) => items.find((item) => item.id === id))
    .filter((item): item is LinkHubItem => Boolean(item));
};

const nowListeningItem = pickItems('music', ['now-listening'])[0];
type SectionPanelType = 'embed' | 'ai-accountability';
type SiteToneKey =
  | 'balance'
  | 'clarity'
  | 'devotion'
  | 'contemplation'
  | 'discernment'
  | 'vitality'
  | 'grounding'
  | 'compassion'
  | 'harmony'
  | 'seva'
  | 'intensity'
  | 'presence';

interface BaseHomeSection {
  id: string;
  label: string;
  color: AccentColorKey;
  tone: SiteToneKey;
  panelType: SectionPanelType;
  isLimited?: boolean;
}

interface EmbedHomeSection extends BaseHomeSection {
  panelType: 'embed';
  items: LinkHubItem[];
}

interface AIAccountabilityHomeSection extends BaseHomeSection {
  panelType: 'ai-accountability';
}

type HomeSection = EmbedHomeSection | AIAccountabilityHomeSection;
const ACCENT_SWATCHES: Record<AccentColorKey, string> = {
  yellow: '#dcc56e',
  blue: '#7cb8c0',
  orange: '#d6a06b',
  red: '#c77b73',
  white: '#eadfc0',
  black: '#6b6158',
  violet: '#a28ed0',
  indigo: '#7392c1'
};
const getAccentSwatch = (value: AccentColorKey) => ACCENT_SWATCHES[value] || ACCENT_SWATCHES.blue;

const homeSections: HomeSection[] = [
  {
    id: 'music',
    label: 'Music',
    color: 'blue',
    tone: 'harmony',
    panelType: 'embed',
    items: pickItems('music', ['my-first-beat-tape', 'everyday-soundscape']).concat(
      pickItems('resources', ['practicing-kriya'])
    )
  },
  {
    id: 'visual',
    label: 'Visual',
    color: 'orange',
    tone: 'clarity',
    panelType: 'embed',
    items: pickItems('social', ['instagram', 'threads', 'tiktok'])
  },
  {
    id: 'video',
    label: 'Video',
    color: 'red',
    tone: 'vitality',
    panelType: 'embed',
    items: pickItems('video', ['everyday-life-video', 'youtube-channel', 'post-labor-econ-playlist'])
  },
  {
    id: 'growth',
    label: 'Growth',
    color: 'white',
    tone: 'presence',
    panelType: 'embed',
    isLimited: true,
    items: pickItems('video', ['growth-happiness-journey'])
  },
  {
    id: 'newark',
    label: 'Newark',
    color: 'yellow',
    tone: 'grounding',
    panelType: 'embed',
    items: pickItems('resources', ['work-archive', 'map', 'mit-living-wage'])
  },
  {
    id: 'scraps',
    label: 'Scraps',
    color: 'black',
    tone: 'contemplation',
    panelType: 'embed',
    items: pickItems('music', ['scraps-2023-2024']).concat(
      pickItems('writing', ['writing-archive', 'substack', 'magazine-publication'])
    )
  },
  {
    id: 'ai-accountability',
    label: 'AI Accountability',
    color: 'orange',
    tone: 'discernment',
    panelType: 'ai-accountability'
  }
];
const shortDateFormatter = new Intl.DateTimeFormat('en-US', {
  month: 'short',
  day: 'numeric',
  year: 'numeric'
});
const pluralize = (count: number, singular: string, plural = `${singular}s`) => `${count} ${count === 1 ? singular : plural}`;
const formatDateLabel = (value: unknown, fallback: string): string => {
  let parsed: Date | null = null;

  if (value instanceof Date) {
    parsed = Number.isNaN(value.valueOf()) ? null : value;
  } else if (typeof value === 'string' || typeof value === 'number') {
    const candidate = new Date(value);
    parsed = Number.isNaN(candidate.valueOf()) ? null : candidate;
  }

  return parsed ? shortDateFormatter.format(parsed) : fallback;
};

const projectCount = allProjects.length;
const projectTypeCount = new Set(allProjects.map((project) => project.data.type)).size;
const postCount = allPosts.length;
const latestPostDateLabel = formatDateLabel(allPosts[0]?.data?.date, 'Archive snapshot');
const roomList = Array.isArray(rooms) ? rooms : [];
const roomCount = roomList.length;
const machineCount = roomList.reduce((count, room) => count + (Array.isArray(room.machines) ? room.machines.length : 0), 0);
const agiSignalCount = Array.isArray(agiTimelineData?.items) ? agiTimelineData.items.length : 0;
const agiUpdatedAtLabel = formatDateLabel(agiTimelineData?.meta?.generatedAt, 'Live sync unavailable - showing cached.');
const reflectionItems = [
  {
    id: 'work',
    title: 'Work',
    href: '/work',
    summary:
      projectCount > 0
        ? `${pluralize(projectCount, 'project')} across ${pluralize(projectTypeCount, 'type')}.`
        : 'No projects indexed yet.',
    meta: projectCount > 0 ? `Coverage: ${pluralize(projectTypeCount, 'type')}` : 'Coverage pending',
    cta: 'Open Work'
  },
  {
    id: 'writing',
    title: 'Writing',
    href: '/writing',
    summary:
      postCount > 0 ? `${pluralize(postCount, 'entry', 'entries')} published. Latest ${latestPostDateLabel}.` : 'No entries yet.',
    meta: postCount > 0 ? `Latest update ${latestPostDateLabel}` : 'Latest update pending',
    cta: 'Open Writing'
  },
  {
    id: 'social',
    title: 'Social',
    href: '/social',
    summary: `${pluralize(roomCount, 'room')} and ${pluralize(machineCount, 'machine')} mapped in the arcade.`,
    meta: `Index: ${pluralize(roomCount, 'room')}`,
    cta: 'Open Social'
  },
  {
    id: 'agi',
    title: 'AGI Timeline',
    href: '/agi',
    summary: agiSignalCount > 0 ? `${pluralize(agiSignalCount, 'signal')} tracked in the monitor.` : 'No AGI signals indexed yet.',
    meta: `Last sync ${agiUpdatedAtLabel}`,
    cta: 'Open AGI Timeline'
  },
  {
    id: 'contact',
    title: 'Contact',
    href: '/contact',
    summary: 'Inquiry form plus 4 quick links for direct outreach.',
    meta: 'Channels: form + socials',
    cta: 'Open Contact'
  }
];
const archivePulseItems = [
  {
    label: 'Threads',
    value: pluralize(threads.length, 'thread')
  },
  {
    label: 'Rooms',
    value: pluralize(roomCount, 'room')
  },
  {
    label: 'Writing archive',
    value: pluralize(postCount, 'entry', 'entries')
  },
  {
    label: 'Last signal',
    value: agiUpdatedAtLabel
  }
];
const xHandle = 'a_e_s_4';
const youtubeChannelId = 'UCQeJiBS72gxrZXw5GmqtocA';
const youtubeUploadsPlaylistId = `UU${youtubeChannelId.slice(2)}`;
const xPublishedAtToTimestamp = (value: string): number => {
  const parsed = Date.parse(value);
  return Number.isNaN(parsed) ? 0 : parsed;
};
const localXFallbackItems = xFallbackItems
  .map((item) => ({
    title: String(item?.title || '').trim(),
    url: String(item?.url || '').trim(),
    publishedAt: String(item?.publishedAt || '').trim(),
    summary: String(item?.summary || '').trim()
  }))
  .filter((item) => item.title && item.url);
const xFeedMode = 'fallback' as const;
const xFeedItems = localXFallbackItems
  .sort((a, b) => xPublishedAtToTimestamp(b.publishedAt) - xPublishedAtToTimestamp(a.publishedAt))
  .slice(0, 6);
const youtubeUploadsEmbedUrl = `https://www.youtube-nocookie.com/embed/videoseries?list=${youtubeUploadsPlaylistId}`;
const latestYoutubeItem = (Array.isArray(youtubeFeed?.items) ? youtubeFeed.items : [])
  .map((item) => ({
    url: typeof item?.url === 'string' ? item.url.trim() : '',
    publishedAt: typeof item?.publishedAt === 'string' ? item.publishedAt : ''
  }))
  .filter((item) => item.url)
  .sort((a, b) => xPublishedAtToTimestamp(b.publishedAt) - xPublishedAtToTimestamp(a.publishedAt))[0];
const youtubePrimaryUrl = latestYoutubeItem?.url || '';
const instagramPreview = (Array.isArray(igManifest) ? igManifest : [])
  .filter((item) => typeof item?.src === 'string' && item.src)
  .slice(0, 4)
  .map((item) => ({
    src: String(item.src),
    caption: typeof item.caption === 'string' ? item.caption : '',
    date: typeof item.date === 'string' ? item.date : ''
  }));
---

<BaseLayout title="A Path Toward Life — Archive" description={profile.short_bio} theme="archive">
  <div class="desktop-home">
    <section class="home-hero">
      <div class="home-hero__content space-y-6 text-center">
        <span class="home-hero__ivy home-hero__ivy--left" aria-hidden="true"></span>
        <span class="home-hero__ivy home-hero__ivy--right" aria-hidden="true"></span>
        <p class="text-xs uppercase tracking-[0.2em] text-zinc-500">Timeline / Archive</p>
        <h1 class="mx-auto max-w-3xl font-display text-3xl font-bold sm:text-4xl md:text-6xl text-accent">A Path Toward Life</h1>
        <p class="mx-auto max-w-4xl text-base sm:text-lg">{profile.long_bio}</p>
        <div class="flex flex-wrap justify-center gap-2" aria-label="Identity chips">
          {profile.roles.map((role) => <span class="chip rounded-full px-3 py-1 text-xs sm:text-sm">{role}</span>)}
        </div>
        <dl class="home-hero__pulse" aria-label="Archive pulse">
          {archivePulseItems.map((item) => (
            <div class="home-hero__pulse-item">
              <dt class="home-hero__pulse-label">{item.label}</dt>
              <dd class="home-hero__pulse-value">{item.value}</dd>
            </div>
          ))}
        </dl>
      </div>
    </section>

    <section class="home-start viewer-only" aria-labelledby="home-start-heading">
      <p class="home-start-kicker">Start here</p>
      <h2 id="home-start-heading" class="home-start-title">Choose your first path</h2>
      <div class="home-start-grid">
        <a class="home-start-card" href="/work">
          <span>Selected Work</span>
          <small>Projects and outcomes</small>
        </a>
        <a class="home-start-card" href="/map">
          <span>Map</span>
          <small>Mind and concept atlas</small>
        </a>
        <a class="home-start-card" href="/now">
          <span>Now</span>
          <small>Current focus and direction</small>
        </a>
        <a class="home-start-card" href="/?lab=1&view=walk">
          <span>Guide</span>
          <small>Walkthrough in Lab Mode</small>
        </a>
      </div>
    </section>

    <section class="mt-12 md:mt-14">
      <h2 class="font-display text-2xl">Featured Threads</h2>
      <div class="mt-4 flex gap-4 overflow-x-auto pb-2 md:grid md:grid-cols-3 md:overflow-visible">
        {threads.map((thread) => (
          <a
            href={thread.href}
            class="card threadCard min-w-[270px] p-4"
            {...(thread.external ? { target: '_blank', rel: 'noopener' } : {})}
          >
            <p class="text-xs uppercase tracking-wide text-zinc-500">{thread.year} · {thread.type}</p>
            <h3 class="mt-1 font-display text-lg">{thread.title}</h3>
            <p class="mt-2 text-sm">{thread.blurb}</p>
            <p class="mt-3 text-sm underline decoration-accent">View thread</p>
          </a>
        ))}
      </div>
    </section>

    <HomeTabReflections items={reflectionItems} sectionTitle="Tab Reflections" variant="desktop" />

    <section class="mt-12 md:mt-14">
      <h2 class="font-display text-2xl">Sections</h2>
      <div class="mt-3 section-tablist" role="tablist" aria-label="Section spotlight tabs">
        {homeSections.map((section, index) => (
          <button
            type="button"
            role="tab"
            style={`--section-accent:${getAccentSwatch(section.color)}`}
            data-section-tab={section.id}
            data-color={section.color}
            data-tone={section.tone}
            aria-label={section.isLimited ? `${section.label}, limited content` : undefined}
            aria-selected={index === 0 ? 'true' : 'false'}
            class:list={['section-tab', index === 0 && 'is-active']}
          >
            <span>{section.label}</span>
            {section.isLimited && <span class="section-tab-badge">Limited</span>}
          </button>
        ))}
      </div>

      <div class="mt-4 section-panels-shell">
        <div class="section-panels">
        {homeSections.map((section, index) => (
          <div
            class:list={['section-panel', index === 0 && 'is-active']}
            style={`--section-accent:${getAccentSwatch(section.color)}`}
            data-section-panel={section.id}
            role="tabpanel"
            aria-label={`${section.label} spotlight`}
          >
            {section.panelType === 'embed' ? (
              section.items.length > 0 ? (
                <div class="section-cards">
                  {section.items.map((item) => (
                    <div class="section-card-shell">
                      <EmbedCard item={item} />
                    </div>
                  ))}
                </div>
              ) : (
                <p class="text-sm text-zinc-400">No spotlight links configured yet.</p>
              )
            ) : (
              <AIAccountabilityPanel />
            )}
          </div>
        ))}
        </div>
      </div>
    </section>

    <HomeSocialFeeds
      xHandle={xHandle}
      xFeedItems={xFeedItems}
      xFeedMode={xFeedMode}
      youtubeUploadsEmbedUrl={youtubeUploadsEmbedUrl}
      youtubePrimaryUrl={youtubePrimaryUrl}
      youtubeChannelUrl={profile.social_links.youtube}
      instagramProfileUrl={profile.social_links.instagram}
      instagramArchiveUrl="/instagram"
      instagramPreview={instagramPreview}
    />

    <UntitledPlayer title="Listen" subtitle="untitled.stream" />
    {nowListeningItem && (
      <section class="mt-4">
        <h2 class="mb-3 font-display text-xl">Now Listening</h2>
        <EmbedCard item={nowListeningItem} />
      </section>
    )}
  </div>

  <script is:inline>
    (() => {
      const tablist = document.querySelector('.section-tablist');
      const tabs = Array.from(document.querySelectorAll('[data-section-tab]'));
      const panels = Array.from(document.querySelectorAll('[data-section-panel]'));
      if (!(tablist instanceof HTMLElement) || !tabs.length || !panels.length) return;

      const activate = (id, syncAccent = true) => {
        tabs.forEach((tab) => {
          const active = tab.getAttribute('data-section-tab') === id;
          tab.classList.toggle('is-active', active);
          tab.setAttribute('aria-selected', String(active));
        });
        panels.forEach((panel) => {
          const active = panel.getAttribute('data-section-panel') === id;
          panel.classList.toggle('is-active', active);
        });
        window.dispatchEvent(new CustomEvent('home:section-activated', { detail: { id } }));

        const activeTab = tabs.find((tab) => tab.getAttribute('data-section-tab') === id);
        if (activeTab instanceof HTMLElement && tablist.scrollWidth > tablist.clientWidth + 1) {
          activeTab.scrollIntoView({
            behavior: syncAccent ? 'smooth' : 'auto',
            block: 'nearest',
            inline: 'center'
          });
        }

        if (!syncAccent) return;
        const color = activeTab?.getAttribute('data-color') || 'blue';
        const tone = activeTab?.getAttribute('data-tone') || 'balance';
        window.__setSiteAccent?.(color, { animate: true, duration: 250 });
        window.__setSiteTone?.(tone, { animate: true });
      };

      tabs.forEach((tab) => {
        tab.addEventListener('click', () => {
          const id = tab.getAttribute('data-section-tab');
          if (id) activate(id);
        });
        tab.addEventListener('keydown', (event) => {
          const currentIndex = tabs.indexOf(tab);
          if (currentIndex === -1) return;

          let nextIndex = -1;
          if (event.key === 'ArrowRight') nextIndex = Math.min(currentIndex + 1, tabs.length - 1);
          if (event.key === 'ArrowLeft') nextIndex = Math.max(currentIndex - 1, 0);
          if (event.key === 'Home') nextIndex = 0;
          if (event.key === 'End') nextIndex = tabs.length - 1;
          if (nextIndex === -1 || nextIndex === currentIndex) return;

          event.preventDefault();
          const nextTab = tabs[nextIndex];
          const id = nextTab?.getAttribute('data-section-tab');
          nextTab?.focus();
          if (id) activate(id);
        });
      });

      const first = tabs[0]?.getAttribute('data-section-tab');
      if (first) activate(first, false);
    })();
  </script>
</BaseLayout>

<style>
  .home-hero {
    position: relative;
    isolation: isolate;
    min-height: clamp(22rem, 42vw, 32rem);
    display: grid;
    place-items: center;
    overflow: hidden;
    border-radius: clamp(1.6rem, 3vw, 2.8rem);
    margin-bottom: clamp(1rem, 2vw, 2rem);
    border: 1px solid color-mix(in srgb, var(--tone-accent) 10%, var(--line2));
    background:
      linear-gradient(180deg, rgba(5, 9, 17, 0.26), rgba(4, 8, 15, 0.74)),
      radial-gradient(
        78% 58% at 50% 4%,
        color-mix(in srgb, var(--tone-accent) 7%, transparent),
        transparent 72%
      ),
      linear-gradient(135deg, rgba(8, 13, 22, 0.26), rgba(4, 8, 15, 0.6)),
      url('/images/bg-station.jpg');
    background-position: center;
    background-size: cover;
    box-shadow:
      0 30px 80px rgba(0, 0, 0, 0.42),
      0 1px 0 rgba(255, 255, 255, 0.03) inset;
  }

  .home-hero::before,
  .home-hero::after {
    content: '';
    position: absolute;
    pointer-events: none;
  }

  .home-hero::before {
    inset: -18% -8% 26%;
    z-index: 0;
    background:
      radial-gradient(
        34% 20% at 50% 22%,
        color-mix(in srgb, var(--tone-accent-bright) 14%, transparent),
        transparent 72%
      ),
      radial-gradient(
        26% 16% at 26% 26%,
        color-mix(in srgb, var(--tone-sky-bright) 10%, transparent),
        transparent 74%
      ),
      radial-gradient(
        26% 16% at 74% 24%,
        color-mix(in srgb, var(--tone-sky-bright) 8%, transparent),
        transparent 74%
      );
    filter: blur(32px);
    opacity: 0.8;
  }

  .home-hero::after {
    inset: 0;
    z-index: 2;
    background:
      linear-gradient(
        115deg,
        transparent 0%,
        transparent 24%,
        rgba(255, 255, 255, 0.04) 34%,
        transparent 46%,
        transparent 100%
      ),
      repeating-linear-gradient(
        90deg,
        rgba(255, 255, 255, 0.022) 0 1px,
        transparent 1px 40px
      );
    mix-blend-mode: screen;
    opacity: 0.18;
  }

  .home-hero__content {
    position: relative;
    isolation: isolate;
    z-index: 4;
    width: min(100%, 56rem);
    padding:
      clamp(4.25rem, 8vw, 6.75rem)
      clamp(1.25rem, 5vw, 4rem)
      clamp(3.25rem, 7vw, 5.5rem);
    text-shadow: 0 10px 30px rgba(0, 0, 0, 0.28);
  }

  .home-hero__content::before,
  .home-hero__content::after {
    content: '';
    position: absolute;
    pointer-events: none;
  }

  .home-hero__content::before {
    z-index: 0;
    inset:
      clamp(1.1rem, 3vw, 2.2rem)
      clamp(0.8rem, 4vw, 2.4rem)
      clamp(1rem, 3vw, 2rem);
    border-radius: clamp(1.35rem, 3vw, 2.2rem);
    background:
      radial-gradient(
        52% 50% at 50% 34%,
        rgba(8, 13, 22, 0.08),
        rgba(8, 13, 22, 0.28) 68%,
        rgba(8, 13, 22, 0.54)
      ),
      linear-gradient(
        180deg,
        rgba(8, 13, 22, 0.03),
        rgba(8, 13, 22, 0.14) 42%,
        rgba(8, 13, 22, 0.3)
      );
    border: 1px solid rgba(255, 255, 255, 0.07);
    backdrop-filter: blur(14px) saturate(1.04);
    box-shadow:
      0 28px 56px rgba(0, 0, 0, 0.24),
      0 1px 0 rgba(255, 255, 255, 0.05) inset;
  }

  .home-hero__content::after {
    z-index: 2;
    inset:
      clamp(1.45rem, 3.4vw, 2.5rem)
      clamp(1.1rem, 4.6vw, 2.8rem)
      clamp(1.35rem, 3.4vw, 2.3rem);
    border-radius: clamp(1.1rem, 2.7vw, 2rem);
    border: 1px solid rgba(255, 255, 255, 0.035);
    background:
      linear-gradient(
        135deg,
        rgba(255, 255, 255, 0.03),
        transparent 22%,
        transparent 74%,
        rgba(242, 184, 75, 0.03)
      );
  }

  .home-hero__content > :not(.home-hero__ivy) {
    position: relative;
    z-index: 3;
  }

  .home-hero__ivy {
    position: absolute;
    top: clamp(0.2rem, 1vw, 0.9rem);
    z-index: 1;
    width: clamp(104px, 14%, 148px);
    height: clamp(136px, 23%, 196px);
    pointer-events: none;
    background-repeat: no-repeat;
    background-size: contain;
    opacity: 0.7;
    filter: saturate(0.7) brightness(0.78) contrast(0.94) drop-shadow(0 10px 14px rgba(0, 0, 0, 0.16));
  }

  .home-hero__ivy--left {
    left: clamp(0.8rem, 2.4vw, 1.6rem);
    background-image: url('/images/garland/left-vine.png');
    background-position: left top;
    transform: rotate(-4deg);
    -webkit-mask-image: linear-gradient(160deg, rgba(0, 0, 0, 0.96) 18%, rgba(0, 0, 0, 0.9) 54%, transparent 100%);
    mask-image: linear-gradient(160deg, rgba(0, 0, 0, 0.96) 18%, rgba(0, 0, 0, 0.9) 54%, transparent 100%);
  }

  .home-hero__ivy--right {
    right: clamp(0.8rem, 2.4vw, 1.6rem);
    background-image: url('/images/garland/right-vine.png');
    background-position: right top;
    transform: rotate(4deg);
    -webkit-mask-image: linear-gradient(200deg, rgba(0, 0, 0, 0.96) 18%, rgba(0, 0, 0, 0.9) 54%, transparent 100%);
    mask-image: linear-gradient(200deg, rgba(0, 0, 0, 0.96) 18%, rgba(0, 0, 0, 0.9) 54%, transparent 100%);
  }

  .home-hero__pulse {
    width: min(100%, 44rem);
    margin: clamp(0.25rem, 1vw, 0.6rem) auto 0;
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: clamp(0.55rem, 1vw, 0.8rem);
  }

  .home-hero__pulse-item {
    margin: 0;
    padding: clamp(0.68rem, 1.2vw, 0.9rem) clamp(0.75rem, 1.4vw, 0.95rem);
    border-radius: 1rem;
    border: 1px solid rgba(255, 255, 255, 0.05);
    background:
      linear-gradient(
        180deg,
        rgba(255, 255, 255, 0.04),
        rgba(255, 255, 255, 0.015)
      ),
      color-mix(in srgb, var(--tone-bg) 84%, rgba(255, 255, 255, 0.02));
    box-shadow:
      0 14px 28px rgba(0, 0, 0, 0.22),
      0 1px 0 rgba(255, 255, 255, 0.03) inset;
    backdrop-filter: blur(12px);
  }

  .home-hero__pulse-label,
  .home-hero__pulse-value {
    margin: 0;
  }

  .home-hero__pulse-label {
    font-size: 0.74rem;
    line-height: 1.2;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: color-mix(in srgb, var(--tone-text) 78%, var(--tone-muted) 22%);
  }

  .home-hero__pulse-value {
    margin-top: 0.32rem;
    font-family: var(--font-display);
    font-size: clamp(0.94rem, 1.7vw, 1.08rem);
    line-height: 1.22;
    color: color-mix(in srgb, var(--tone-accent-glow) 92%, white 8%);
  }

  .home-start {
    margin-top: clamp(1rem, 2.4vw, 1.6rem);
    padding: clamp(0.95rem, 1.8vw, 1.2rem);
    border: 1px solid color-mix(in srgb, var(--tone-accent) 22%, var(--line2));
    border-radius: 1rem;
    background:
      linear-gradient(180deg, color-mix(in srgb, var(--tone-accent) 11%, transparent), transparent 62%),
      color-mix(in srgb, var(--tone-surface-strong) 88%, transparent);
  }

  .home-start-kicker,
  .home-start-title {
    margin: 0;
  }

  .home-start-kicker {
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: color-mix(in srgb, var(--tone-text) 84%, var(--tone-muted) 16%);
  }

  .home-start-title {
    margin-top: 0.35rem;
    font-size: clamp(1.2rem, 2vw, 1.6rem);
    line-height: 1.15;
  }

  .home-start-grid {
    margin-top: 0.75rem;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 0.6rem;
  }

  .home-start-card {
    border: 1px solid color-mix(in srgb, var(--tone-accent) 24%, var(--line2));
    border-radius: 0.82rem;
    padding: 0.7rem 0.74rem;
    display: grid;
    gap: 0.28rem;
    text-decoration: none;
    color: color-mix(in srgb, var(--tone-text) 92%, var(--tone-muted) 8%);
    background:
      linear-gradient(180deg, color-mix(in srgb, var(--tone-accent) 16%, transparent), transparent 64%),
      color-mix(in srgb, var(--tone-surface) 90%, transparent);
    transition:
      border-color 140ms ease,
      transform 140ms ease,
      color 140ms ease;
  }

  .home-start-card span {
    font-family: var(--font-display);
    font-size: 0.98rem;
    line-height: 1.2;
  }

  .home-start-card small {
    color: color-mix(in srgb, var(--tone-text) 80%, var(--tone-muted) 20%);
    font-size: 0.76rem;
    line-height: 1.4;
  }

  .home-start-card:hover,
  .home-start-card:focus-visible {
    border-color: color-mix(in srgb, var(--tone-accent) 58%, var(--line2));
    color: var(--tone-accent-glow);
    transform: translateY(-1px);
  }

  .section-tablist {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: clamp(0.55rem, 1vw, 0.85rem);
    padding-bottom: 0.16rem;
  }

  .section-tab {
    position: relative;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.35rem;
    min-height: 40px;
    padding: 0.3rem 0.82rem;
    border: 1px solid color-mix(in srgb, var(--section-accent, var(--tone-accent)) 48%, transparent);
    border-radius: 999px;
    background:
      linear-gradient(
        180deg,
        color-mix(in oklab, var(--section-accent, var(--tone-accent)) 20%, transparent),
        color-mix(in oklab, var(--section-accent, var(--tone-accent)) 11%, transparent)
      );
    box-shadow:
      inset 0 1px 0 color-mix(in srgb, white 8%, transparent),
      0 0 0 1px color-mix(in srgb, var(--section-accent, var(--tone-accent)) 12%, transparent);
    backdrop-filter: none;
    color: color-mix(in srgb, var(--tone-text) 44%, var(--section-accent, var(--tone-accent)) 56%);
    font-size: 0.875rem;
    line-height: 1.15;
    font-weight: 600;
    cursor: pointer;
    transition:
      color var(--motion-fast) var(--ease-standard),
      background-color var(--motion-base) var(--ease-standard),
      transform var(--motion-fast) var(--ease-emphasized);
  }

  .section-tab-badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    border: 1px solid color-mix(in srgb, var(--section-accent, var(--tone-accent)) 68%, transparent);
    background: color-mix(in srgb, var(--section-accent, var(--tone-accent)) 24%, transparent);
    color: color-mix(in srgb, var(--tone-text) 82%, var(--section-accent, var(--tone-accent)) 18%);
    font-size: 0.68rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    line-height: 1;
    padding: 0.18rem 0.4rem;
  }

  .section-tab::after {
    content: none;
  }

  .section-tab:hover,
  .section-tab:focus-visible,
  .section-tab.is-active {
    color: color-mix(in srgb, var(--section-accent, var(--tone-accent-glow)) 84%, white 16%);
    border-color: color-mix(in srgb, var(--section-accent, var(--tone-accent)) 82%, transparent);
    background:
      linear-gradient(
        180deg,
        color-mix(in srgb, var(--section-accent, var(--tone-accent)) 30%, transparent),
        color-mix(in srgb, var(--section-accent, var(--tone-accent)) 18%, transparent)
      );
    box-shadow:
      inset 0 1px 0 color-mix(in srgb, white 9%, transparent),
      0 6px 16px color-mix(in srgb, var(--section-accent, var(--tone-accent)) 9%, transparent);
  }

  .section-tab:focus-visible {
    outline: 2px solid color-mix(in srgb, var(--tone-accent) 85%, transparent);
    outline-offset: 2px;
  }

  .section-panel {
    display: none;
    overflow-x: visible;
    padding: 0.95rem 1rem 0.3rem;
    border: 1px solid color-mix(in srgb, var(--section-accent, var(--tone-accent)) 28%, transparent);
    background:
      linear-gradient(
        180deg,
        color-mix(in srgb, var(--section-accent, var(--tone-accent)) 10%, transparent),
        color-mix(in srgb, var(--section-accent, var(--tone-accent)) 5%, transparent) 26%,
        transparent 52%
      );
    border-radius: 1.08rem;
    box-shadow:
      inset 3px 0 0 color-mix(in srgb, var(--section-accent, var(--tone-accent)) 58%, transparent),
      inset 0 1px 0 color-mix(in srgb, white 5%, transparent);
  }

  .section-panel.is-active {
    display: block;
    animation: homeSectionPanelIn 220ms var(--ease-emphasized);
  }

  .section-panels-shell {
    display: grid;
    gap: 0.5rem;
  }

  .section-cards {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.75rem;
    align-items: stretch;
    width: min(100%, 78rem);
    margin-inline: auto;
    min-width: 0;
  }

  .section-card-shell {
    width: 100%;
    min-width: 0;
    height: 100%;
    max-width: 100%;
  }

  .section-card-shell :global(.hub-card) {
    height: 100%;
  }

  .section-card-shell :global(.hub-embed) {
    min-width: 100%;
  }

  @media (min-width: 861px) and (max-width: 1279px) {
    .section-cards {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 860px) {
    .home-hero {
      min-height: 20rem;
      border-radius: 1.35rem;
      margin-bottom: 0.8rem;
    }

    .home-hero__content {
      width: min(100%, 38rem);
      padding:
        clamp(3.5rem, 11vw, 4.6rem)
        clamp(1rem, 5vw, 1.4rem)
        clamp(2.25rem, 9vw, 3rem);
    }

    .home-hero__content::before {
      inset:
        1rem
        0.5rem
        0.75rem;
      border-radius: 1.1rem;
    }

    .home-hero__content::after {
      inset:
        1.25rem
        0.8rem
        1rem;
      border-radius: 0.95rem;
    }

    .home-hero__ivy {
      top: 0.3rem;
      width: clamp(72px, 18vw, 104px);
      height: clamp(94px, 24vw, 136px);
      opacity: 0.66;
    }

    .home-hero__ivy--left {
      left: 0.5rem;
    }

    .home-hero__ivy--right {
      right: 0.5rem;
    }

    .home-hero__pulse {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .threadCard {
      min-width: min(84vw, 24rem);
    }

    .section-tablist {
      flex-wrap: nowrap;
      overflow-x: auto;
      overscroll-behavior-x: contain;
      touch-action: pan-x;
      gap: 0.6rem;
      margin-inline: -0.05rem;
      padding-inline: 0.05rem;
      padding-bottom: 0.42rem;
      scrollbar-width: thin;
    }

    .section-tab {
      flex: 0 0 auto;
      min-height: 38px;
      padding: 0.18rem 0.62rem;
      white-space: nowrap;
    }

    .section-cards {
      display: flex;
      flex-wrap: nowrap;
      overflow-x: auto;
      scroll-snap-type: x mandatory;
      overscroll-behavior-x: contain;
      width: 100%;
      margin-inline: 0;
      padding-bottom: 0.38rem;
    }

    .section-card-shell {
      flex: 0 0 auto;
      width: min(88vw, 24rem);
      min-width: min(88vw, 24rem);
      height: auto;
      scroll-snap-align: start;
    }
  }

  @media (max-width: 520px) {
    .home-hero__pulse {
      grid-template-columns: 1fr;
    }

    .section-cards {
      flex-wrap: wrap;
      overflow-x: visible;
      scroll-snap-type: none;
    }

    .section-card-shell {
      flex-basis: 100%;
      width: 100%;
      min-width: 100%;
    }
  }

  @media (max-width: 640px) {
    .home-hero {
      min-height: auto;
      border-radius: 1.05rem;
      background:
        linear-gradient(180deg, rgba(5, 9, 17, 0.38), rgba(4, 8, 15, 0.84)),
        linear-gradient(135deg, rgba(8, 13, 22, 0.34), rgba(4, 8, 15, 0.72)),
        url('/images/bg-station.jpg');
      box-shadow:
        0 18px 42px rgba(0, 0, 0, 0.34),
        0 1px 0 rgba(255, 255, 255, 0.03) inset;
    }

    .home-hero::before,
    .home-hero::after,
    .home-hero__content::after,
    .home-hero__ivy {
      display: none;
    }

    .home-hero__content {
      width: 100%;
      padding:
        2.8rem
        0.7rem
        1.45rem;
      text-shadow: 0 8px 24px rgba(0, 0, 0, 0.24);
    }

    .home-hero__content::before {
      inset:
        0.7rem
        0.2rem
        0.35rem;
      border-radius: 0.92rem;
      background:
        linear-gradient(
          180deg,
          rgba(8, 13, 22, 0.12),
          rgba(8, 13, 22, 0.34) 60%,
          rgba(8, 13, 22, 0.56)
        );
      backdrop-filter: none;
      box-shadow:
        0 16px 32px rgba(0, 0, 0, 0.2),
        0 1px 0 rgba(255, 255, 255, 0.04) inset;
    }

    .home-hero__pulse-item {
      backdrop-filter: none;
      padding: 0.62rem 0.7rem;
    }
  }

  @keyframes homeSectionPanelIn {
    from {
      opacity: 0;
      transform: translateY(6px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .section-panel.is-active {
      animation: none;
    }
  }
</style>
