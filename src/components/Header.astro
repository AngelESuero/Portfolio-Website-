---
type NavItem = {
  label: string;
  href: string;
  color: string;
  tone: string;
  view?: string;
};
import { focusLanes } from '../data/focus-lanes';

const ACCENT_SWATCHES: Record<string, string> = {
  ochre: '#b97a2d',
  blue: '#587a95',
  red: '#a64a3b',
  white: '#ede3d1',
  black: '#171715'
};

const getAccentSwatch = (value?: string) =>
  ACCENT_SWATCHES[String(value || '').trim().toLowerCase()] || ACCENT_SWATCHES.blue;

const primaryNav: NavItem[] = [
  { label: 'Home', href: '/', color: 'ochre', tone: 'presence' },
  { label: 'Work', href: '/work', color: 'ochre', tone: 'seva' },
  { label: 'Writing', href: '/writing', color: 'white', tone: 'contemplation' },
  { label: 'Archive', href: '/archive', color: 'blue', tone: 'clarity' },
  { label: 'About', href: '/about', color: 'white', tone: 'presence' },
  { label: 'Contact', href: '/contact', color: 'white', tone: 'harmony' }
];

const secondaryNav: NavItem[] = [
  { label: 'Studio Notes', href: '/studio-notes', color: 'white', tone: 'contemplation' },
  { label: 'Collaborations', href: '/collaborations', color: 'ochre', tone: 'grounding' },
  { label: 'Theater', href: '/theater', color: 'red', tone: 'presence' },
  { label: 'Map', href: '/map', color: 'blue', tone: 'presence' },
  { label: 'Map #2', href: '/map?view=unfurling', color: 'white', tone: 'clarity', view: 'unfurling' },
  { label: 'Genre Tree', href: '/genre-tree', color: 'ochre', tone: 'grounding' },
  { label: 'Market Model', href: '/market-model', color: 'blue', tone: 'clarity' },
  { label: 'Resources', href: '/resources', color: 'white', tone: 'contemplation' }
];

const focusNav: NavItem[] = [
  ...focusLanes.map((lane) => ({
    label: lane.label,
    href: `/focus/${lane.slug}`,
    color: lane.navColor,
    tone: lane.navTone
  })),
  { label: 'Career Search', href: '/rejections', color: 'red', tone: 'grounding' }
];

const aiGroup: NavItem[] = [
  { label: 'AI', href: '/ai', color: 'blue', tone: 'discernment' },
  { label: 'AI x Survival', href: '/survival-os', color: 'red', tone: 'grounding' },
  { label: 'Survival Support', href: '/survival-support', color: 'ochre', tone: 'grounding' },
];

const problemsNav: NavItem[] = [
  { label: 'Newark', href: '/newark', color: 'ochre', tone: 'grounding' },
];

import youtubeData from '../data/youtube.json';
const latestVideoId = youtubeData.items[0]?.videoId ?? '';
const latestVideoTitle = youtubeData.items[0]?.title ?? 'Latest video';

const currentPath = Astro.url.pathname.replace(/\/+$/, '') || '/';
const currentView = Astro.url.searchParams.get('view') || '';
const normalize = (path: string) => path.replace(/\/+$/, '') || '/';
const hrefPath = (href: string) => normalize(href.split('?')[0] || href);
const isActive = (item: NavItem) => {
  const target = hrefPath(item.href);
  const pathMatch = currentPath === target || (target !== '/' && currentPath.startsWith(`${target}/`));

  if (!pathMatch) return false;

  if (target === '/map') {
    if (item.view) return currentView === item.view;
    return currentView === '';
  }

  return true;
};
---

<header class="site-header sticky top-0 z-50">
  <div class="site-shell-row header-row flex w-full items-center justify-between py-3 md:py-4">
    <a href="/" class="header-brand font-display text-2xl font-bold tracking-tight text-accent">A<span class="header-brand__micro">es's</span> Path Toward Life</a>
    <nav aria-label="Main navigation" class="header-nav hidden md:flex md:items-center">
      {primaryNav.map((item) => (
        <a
          href={item.href}
          class="header-link text-sm no-underline"
          style={`--link-accent:${getAccentSwatch(item.color)}`}
          {...(item.color ? { 'data-color': item.color } : {})}
          {...(item.tone ? { 'data-tone': item.tone } : {})}
          {...(isActive(item) ? { 'aria-current': 'page' } : {})}
        >
          {item.label}
        </a>
      ))}
    </nav>
    <div class="header-actions flex items-center gap-2">
      <button
        data-music-bar-header-toggle
        aria-label="Toggle music player"
        title="Toggle music player"
        class="pill rounded px-3 py-1 text-sm"
        type="button"
      >
        ♫
      </button>
      <button
        data-youtube-pip-trigger
        data-video-id={latestVideoId}
        aria-label={`Watch latest upload: ${latestVideoTitle}`}
        title={`Latest upload: ${latestVideoTitle}`}
        class="pill rounded px-3 py-1 text-sm"
        type="button"
      >
        ▶
      </button>
      <button
        id="secondary-nav-toggle"
        aria-expanded="false"
        aria-controls="header-secondary-nav"
        title="More navigation"
        class="pill rounded px-3 py-1 text-sm"
        type="button"
      >
        <span class="secondary-nav-toggle__icon">⊞</span>
      </button>
      <button
        id="mobile-nav-toggle"
        aria-expanded="false"
        aria-controls="mobile-nav"
        class="pill rounded px-3 py-1 text-sm md:hidden"
        type="button"
      >
        Menu
      </button>
    </div>
  </div>
  <div class="site-shell-row header-secondary" id="header-secondary-nav" data-secondary-nav>
    <nav aria-label="Secondary navigation" class="header-secondary-nav">
      <div class="header-secondary-group">
        <p class="header-secondary-label">Elsewhere</p>
        <div class="header-secondary-group__items">
          <span class="header-ai-group">
            {aiGroup.map((item) => (
              <a
                href={item.href}
                class="header-link header-link--secondary text-sm no-underline"
                style={`--link-accent:${getAccentSwatch(item.color)}`}
                {...(item.color ? { 'data-color': item.color } : {})}
                {...(item.tone ? { 'data-tone': item.tone } : {})}
                {...(isActive(item) ? { 'aria-current': 'page' } : {})}
              >
                {item.label}
              </a>
            ))}
          </span>
          {secondaryNav.map((item) => (
            <a
              href={item.href}
              class="header-link header-link--secondary text-sm no-underline"
              style={`--link-accent:${getAccentSwatch(item.color)}`}
              {...(item.color ? { 'data-color': item.color } : {})}
              {...(item.tone ? { 'data-tone': item.tone } : {})}
              {...(isActive(item) ? { 'aria-current': 'page' } : {})}
            >
              {item.label}
            </a>
          ))}
        </div>
      </div>
      <div class="header-secondary-group header-secondary-group--practice">
        <p class="header-secondary-label">Practice</p>
        <div class="header-secondary-group__items">
          {focusNav.map((item) => (
            <a
              href={item.href}
              class="header-link header-link--secondary text-sm no-underline"
              style={`--link-accent:${getAccentSwatch(item.color)}`}
              {...(item.color ? { 'data-color': item.color } : {})}
              {...(item.tone ? { 'data-tone': item.tone } : {})}
              {...(isActive(item) ? { 'aria-current': 'page' } : {})}
            >
              {item.label}
            </a>
          ))}
        </div>
      </div>
      <div class="header-secondary-group header-secondary-group--problems">
        <p class="header-secondary-label">Problems</p>
        <div class="header-secondary-group__items">
          {problemsNav.map((item) => (
            <a
              href={item.href}
              class="header-link header-link--secondary text-sm no-underline"
              style={`--link-accent:${getAccentSwatch(item.color)}`}
              {...(item.color ? { 'data-color': item.color } : {})}
              {...(item.tone ? { 'data-tone': item.tone } : {})}
              {...(isActive(item) ? { 'aria-current': 'page' } : {})}
            >
              {item.label}
            </a>
          ))}
        </div>
      </div>
    </nav>
  </div>
  <nav id="mobile-nav" aria-label="Mobile navigation" class="mobile-nav md:hidden" hidden>
    <div class="site-shell-row mobile-nav-inner">
      {primaryNav.map((item) => (
        <a
          href={item.href}
          class="header-link mobile-nav-link no-underline"
          style={`--link-accent:${getAccentSwatch(item.color)}`}
          {...(item.color ? { 'data-color': item.color } : {})}
          {...(item.tone ? { 'data-tone': item.tone } : {})}
          {...(isActive(item) ? { 'aria-current': 'page' } : {})}
        >
          {item.label}
        </a>
      ))}
      <div class="mobile-nav-section">
        <p class="mobile-nav-label">Elsewhere</p>
        <div class="mobile-ai-group">
          {aiGroup.map((item) => (
            <a
              href={item.href}
              class="header-link mobile-nav-link header-link--secondary no-underline"
              style={`--link-accent:${getAccentSwatch(item.color)}`}
              {...(item.color ? { 'data-color': item.color } : {})}
              {...(item.tone ? { 'data-tone': item.tone } : {})}
              {...(isActive(item) ? { 'aria-current': 'page' } : {})}
            >
              {item.label}
            </a>
          ))}
        </div>
        {secondaryNav.map((item) => (
          <a
            href={item.href}
            class="header-link mobile-nav-link header-link--secondary no-underline"
            style={`--link-accent:${getAccentSwatch(item.color)}`}
            {...(item.color ? { 'data-color': item.color } : {})}
            {...(item.tone ? { 'data-tone': item.tone } : {})}
            {...(isActive(item) ? { 'aria-current': 'page' } : {})}
          >
            {item.label}
          </a>
        ))}
        <p class="mobile-nav-label">Practice</p>
        {focusNav.map((item) => (
          <a
            href={item.href}
            class="header-link mobile-nav-link header-link--secondary no-underline"
            style={`--link-accent:${getAccentSwatch(item.color)}`}
            {...(item.color ? { 'data-color': item.color } : {})}
            {...(item.tone ? { 'data-tone': item.tone } : {})}
            {...(isActive(item) ? { 'aria-current': 'page' } : {})}
          >
            {item.label}
          </a>
        ))}
        <p class="mobile-nav-label">Problems</p>
        {problemsNav.map((item) => (
          <a
            href={item.href}
            class="header-link mobile-nav-link header-link--secondary no-underline"
            style={`--link-accent:${getAccentSwatch(item.color)}`}
            {...(item.color ? { 'data-color': item.color } : {})}
            {...(item.tone ? { 'data-tone': item.tone } : {})}
            {...(isActive(item) ? { 'aria-current': 'page' } : {})}
          >
            {item.label}
          </a>
        ))}
      </div>
    </div>
  </nav>
</header>

<style>
  #secondary-nav-toggle {
    display: none;
  }
  @media (min-width: 768px) {
    #secondary-nav-toggle {
      display: inline-flex;
    }
  }
</style>

<script>
  const initSecondaryNav = () => {
    const btn = document.getElementById('secondary-nav-toggle');
    const nav = document.getElementById('header-secondary-nav');
    if (!btn || !nav) return;

    const STORAGE_KEY = 'secondary-nav-open';

    const setOpen = (open) => {
      nav.classList.toggle('is-open', open);
      btn.setAttribute('aria-expanded', String(open));
      btn.querySelector('.secondary-nav-toggle__icon').textContent = open ? '⊟' : '⊞';
      sessionStorage.setItem(STORAGE_KEY, String(open));
    };

    setOpen(sessionStorage.getItem(STORAGE_KEY) === 'true');
    btn.addEventListener('click', () => setOpen(!nav.classList.contains('is-open')));
  };

  document.addEventListener('astro:page-load', initSecondaryNav);
  initSecondaryNav();
</script>
