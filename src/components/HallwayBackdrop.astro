---
const { accent = '#7BE7FF' } = Astro.props;
---

<div class="hallway" style={`--accent:${accent};--complement:var(--cursor);`} aria-hidden="true">
  <div class="hallway__photo"></div>
  <div class="hallway__focusField"></div>
  <div class="hallway__vignette"></div>
  <div class="hallway__fog"></div>
  <div class="hallway__atmosphere"></div>
  <div class="hallway__depth"></div>
  <div class="hallway__walls"></div>
  <div class="hallway__neon"></div>
  <div class="hallway__grain"></div>
</div>

<style>
  .hallway {
    --focus-x: 50vw;
    --focus-y: 42vh;
    --ambient-focus: 0;
    position: fixed;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    overflow: hidden;
  }

  .hallway__photo {
    position: absolute;
    inset: -10%;
    background-image:
      radial-gradient(
        900px 420px at 58% 16%,
        color-mix(in oklab, var(--complement) 30%, transparent),
        transparent 62%
      ),
      linear-gradient(180deg, rgba(4, 9, 18, 0.5) 0%, rgba(4, 8, 15, 0.72) 48%, rgba(3, 6, 12, 0.88) 100%),
      var(--hallway-photo-url);
    background-position: center;
    background-size: cover;
    background-repeat: no-repeat;
    filter: saturate(1.06) contrast(1.04) brightness(0.54);
    opacity: 0.28;
    transform: scale(1.03);
    transition: opacity 9000ms linear, filter 9000ms ease;
  }

  .hallway__focusField {
    position: absolute;
    inset: -8%;
    z-index: 1;
    background:
      radial-gradient(
        420px 280px at var(--focus-x) var(--focus-y),
        rgb(255 255 255 / 0.14),
        rgb(198 225 255 / 0.08) 18%,
        transparent 62%
      ),
      radial-gradient(
        860px 560px at var(--focus-x) var(--focus-y),
        color-mix(in oklab, var(--accent) 14%, transparent),
        transparent 72%
      ),
      repeating-radial-gradient(
        circle at var(--focus-x) var(--focus-y),
        rgb(255 255 255 / 0.022) 0 1px,
        transparent 1px 18px
      );
    filter: blur(8px);
    opacity: 0.38;
    mix-blend-mode: screen;
    transition: opacity 10000ms linear, filter 9000ms ease;
  }

  .hallway__vignette {
    position: absolute;
    inset: -10%;
    background: radial-gradient(closest-side at var(--focus-x) var(--focus-y), transparent 45%, rgba(0, 0, 0, 0.74) 92%);
    opacity: 0.34;
  }

  .hallway__fog {
    position: absolute;
    inset: -20%;
    background:
      radial-gradient(600px 260px at 35% 35%, rgba(255, 255, 255, 0.055), transparent 62%),
      radial-gradient(700px 320px at 70% 45%, rgba(255, 255, 255, 0.046), transparent 67%),
      radial-gradient(900px 420px at 50% 70%, rgba(255, 255, 255, 0.034), transparent 72%);
    filter: blur(11px);
    opacity: 0.14;
    animation: hallwayFogDrift 28s ease-in-out infinite alternate;
  }

  .hallway__atmosphere {
    position: absolute;
    inset: -18%;
    background:
      radial-gradient(
        780px 420px at 15% 18%,
        color-mix(in oklab, var(--accent) 20%, transparent),
        transparent 70%
      ),
      radial-gradient(
        840px 460px at 84% 24%,
        color-mix(in oklab, var(--complement) 20%, transparent),
        transparent 72%
      ),
      radial-gradient(
        1000px 520px at 50% 82%,
        rgba(255, 255, 255, 0.03),
        transparent 72%
      );
    opacity: 0.07;
    filter: blur(18px) saturate(1.08);
    animation: hallwayBreathe 24s ease-in-out infinite;
  }

  .hallway__depth {
    position: absolute;
    inset: -16%;
    background:
      radial-gradient(85% 58% at 50% 62%, rgba(255, 255, 255, 0.05), transparent 75%),
      repeating-linear-gradient(
        90deg,
        color-mix(in oklab, var(--complement) 9%, transparent) 0 1px,
        transparent 1px 22px
      ),
      repeating-linear-gradient(
        0deg,
        color-mix(in oklab, var(--accent) 8%, transparent) 0 1px,
        transparent 1px 28px
      );
    mix-blend-mode: soft-light;
    opacity: 0.16;
  }

  .hallway__walls {
    position: absolute;
    inset: -30%;
    background:
      linear-gradient(
        90deg,
        color-mix(in oklab, var(--complement) 20%, transparent),
        transparent 24%,
        transparent 76%,
        color-mix(in oklab, var(--accent) 20%, transparent)
      ),
      linear-gradient(
        180deg,
        rgba(0, 0, 0, 0),
        rgba(0, 0, 0, 0.32) 52%,
        rgba(0, 0, 0, 0.67)
      );
    transform: perspective(900px) rotateX(8deg);
    opacity: 0.18;
    transition: opacity 10000ms linear, transform 14000ms cubic-bezier(0.18, 0.84, 0.2, 1);
  }

  .hallway__neon {
    position: absolute;
    inset: -10%;
    background:
      radial-gradient(
        600px 220px at 50% 12%,
        color-mix(in oklab, var(--complement) 30%, transparent),
        transparent 60%
      ),
      radial-gradient(
        800px 360px at 50% 20%,
        color-mix(in oklab, var(--accent) 30%, transparent),
        transparent 70%
      ),
      linear-gradient(
        90deg,
        color-mix(in oklab, var(--complement) 16%, transparent),
        transparent 24%,
        transparent 76%,
        color-mix(in oklab, var(--accent) 16%, transparent)
      );
    filter: blur(1px);
    opacity: 0.16;
    transition: opacity 9000ms linear, filter 9000ms ease;
  }

  .hallway__grain {
    position: absolute;
    inset: -10%;
    background-image:
      repeating-linear-gradient(
        0deg,
        rgba(255, 255, 255, 0.03) 0px,
        rgba(255, 255, 255, 0.03) 1px,
        transparent 2px,
        transparent 4px
      ),
      repeating-linear-gradient(
        90deg,
        rgba(255, 255, 255, 0.02) 0px,
        rgba(255, 255, 255, 0.02) 1px,
        transparent 2px,
        transparent 6px
      );
    mix-blend-mode: overlay;
    opacity: 0.06;
    animation: hallwayBreathe 18s ease-in-out infinite reverse;
  }

  @keyframes hallwayFogDrift {
    from {
      transform: translate3d(-0.8%, -0.5%, 0) scale(1.01);
    }

    to {
      transform: translate3d(0.8%, 0.6%, 0) scale(1.04);
    }
  }

  @keyframes hallwayBreathe {
    0%,
    100% {
      opacity: 0.06;
    }

    50% {
      opacity: 0.11;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hallway__fog,
    .hallway__atmosphere,
    .hallway__grain {
      animation: none;
    }

    .hallway__walls {
      transform: perspective(900px) rotateX(8deg);
    }
  }

  @media (max-width: 767px), (pointer: coarse) {
    .hallway__focusField,
    .hallway__neon,
    .hallway__grain {
      display: none;
    }

    .hallway__photo {
      inset: 0;
      opacity: 0.28;
      filter: saturate(0.9) contrast(1) brightness(0.42);
      transform: none;
      transition: opacity 320ms ease;
    }

    .hallway__fog,
    .hallway__atmosphere,
    .hallway__depth {
      opacity: 0.04;
    }

    .hallway__walls {
      opacity: 0.08;
    }
  }
</style>
