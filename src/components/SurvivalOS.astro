---
import { SURVIVAL_OS } from '../data/survival-os';

const tuneUps = [
  {
    claim: 'WHO annual PM2.5 guideline: 5 µg/m³.',
    note: 'Use this as the primary benchmark for long-run air quality framing.',
    source: 'https://iris.who.int/handle/10665/345329'
  },
  {
    claim: 'Clean cooking gap is commonly cited at ~2.1B people (not 2.4B).',
    note: 'Use latest WHO-aligned figure and include the publication date in the caption.',
    source: 'https://www.who.int/news-room/fact-sheets/detail/household-air-pollution-and-health'
  },
  {
    claim: 'Emergency water minimum in Sphere: 15 L/person/day.',
    note: 'State this as humanitarian minimum; keep survival minimum separate when relevant.',
    source: 'https://disasterlaw.ifrc.org/sites/default/files/media/disaster_law/2021-01/Sphere-Handbook-2018-EN.pdf'
  },
  {
    claim: 'UNICEF Gaza citation: ~1.5–2 L/day observed; 15 L/day humanitarian minimum.',
    note: 'Present both observed reality and humanitarian standard in the same sentence.',
    source: 'https://www.unicef.org/press-releases/barely-drop-drink-children-gaza-strip-do-not-access-90-cent-their-normal-water-use'
  },
  {
    claim: 'Texas Uri death phrasing: “246 officially reported deaths.”',
    note: 'If you include alternative estimates, label them as separate study estimates.',
    source: 'https://www.airclim.org/working-areas/air-pollution/policy-initiatives/who-air-quality-guidelines'
  }
];

const stackModelRows = [
  {
    layer: 'L0: Life Support',
    focus: 'Preserve human viability in the next 24-72 hours.',
    systems: 'Air, potable water, thermoregulation/shelter, calories, essential meds',
    aiRole: 'Sense-and-respond only: detection, alerting, routing, and verification loops.',
    monitor: 'Potable liters/person/day, PM2.5 exceedance days, heat-risk outage hours'
  },
  {
    layer: 'L1: Protection',
    focus: 'Prevent avoidable mortality from exposure, injury, and infection.',
    systems: 'Sanitation, violence/exposure safety, triage, acute care access',
    aiRole: 'Copilot for triage and dispatch with human override on critical decisions.',
    monitor: 'Response time to incidents, ER/clinic wait times, outbreak early-warning signals'
  },
  {
    layer: 'L2: Backbone',
    focus: 'Keep critical infrastructure reliable under stress at population scale.',
    systems: 'Energy, logistics, telecom/comms, data systems, operating governance',
    aiRole: 'Forecasting, maintenance prioritization, and resource allocation with audits.',
    monitor: 'Grid downtime (SAIDI/SAIFI), network uptime, stock-cover days, incident MTTR'
  },
  {
    layer: 'L3: Social Stability',
    focus: 'Maintain institutional trust, legitimacy, and coordinated response.',
    systems: 'Public communication, legal process, benefits delivery, dispute resolution',
    aiRole: 'Decision support with strict transparency, appealability, and liability controls.',
    monitor: 'Benefit/payment delays, grievance backlog, misinformation and unrest spikes'
  },
  {
    layer: 'L4: Living',
    focus: 'Enable dignity, creativity, and long-run flourishing after stability.',
    systems: 'Your existing 9-part living model (education, culture, meaning, connection)',
    aiRole: 'Personalization and creative leverage, constrained by equity guardrails.',
    monitor: 'Participation, learning continuity, mental-health access, mobility and opportunity'
  }
];

const transitionBridgeRows = [
  {
    signal: 'Assist strong, Agent still brittle',
    posture: 'Harden L0-L2 operations before scaling autonomy.',
    gate: 'Require QA coverage, incident logging, and rollback paths for every automation.'
  },
  {
    signal: 'Agent strength rising with reliability gaps',
    posture: 'Deploy supervised automation in L2 first, then L1.',
    gate: 'No unsupervised deployment into triage/safety workflows without exception handling.'
  },
  {
    signal: 'Robots coupling into physical systems',
    posture: 'Treat reliability as life-critical infrastructure.',
    gate: 'Drill manual fallback and fail-safe procedures before expanding fleet autonomy.'
  },
  {
    signal: 'Rules and audits becoming enforceable',
    posture: 'Institutionalize accountability across operators and vendors.',
    gate: 'Standardize audit trails, liability ownership, and appeal workflows.'
  },
  {
    signal: 'Dividends/equity pressure rising',
    posture: 'Protect social stability while productivity scales.',
    gate: 'Track distribution lag and activate ownership/benefit rails before trust erosion.'
  }
];
---

<section class="view-surface">
  <header class="view-surface-header">
    <p class="view-surface-kicker viewer-only">Drafted with AI support for clarity; reviewed and maintained by Angel.</p>
    <p class="view-surface-kicker lab-only">AI-assisted draft provenance and editorial controls.</p>
    <h1>Survival OS + AI Transition</h1>
    <p>
      One operating model: transition signals mapped to survival risk, infrastructure reliability, and social stability.
    </p>
  </header>

  <div class="explore-grid mt-4">
    <a class="explore-card" href="#infographic">Transition dashboard</a>
    <a class="explore-card" href="#integration">Control logic</a>
    <a class="explore-card" href="#stack-model">Integrated stack</a>
    <a class="explore-card" href="/survival-support">Survival support guide</a>
    <a class="explore-card" href="#systems">Vital systems map</a>
    <a class="explore-card" href="#sources">Citations</a>
  </div>
</section>

<section id="infographic" class="view-surface mt-4">
  <header class="view-surface-header">
    <p class="view-surface-kicker">Visual overview</p>
    <h1>Transition Signal Checklist</h1>
    <p>A weekly tracker for whether transition talk is turning into actual movement and delivery.</p>
  </header>

  <div class="card mt-4 p-4">
    <div class="mt-4 flex flex-wrap gap-3">
      <a class="pill rounded-full px-3 py-1.5 text-sm no-underline" href="/ai-waves?v=embedfix2" target="_blank" rel="noreferrer">
        Open standalone checklist
      </a>
    </div>

    <div class="mt-4">
      <iframe
        title="Transition Signal Checklist"
        src="/ai-waves?embed=1&v=embedfix2"
        class="h-[84vh] w-full rounded-xl border"
        loading="lazy"
      ></iframe>
    </div>
  </div>
</section>

<section id="integration" class="view-surface mt-4">
  <header class="view-surface-header">
    <p class="view-surface-kicker">Integration layer</p>
    <h1>Transition-to-survival control logic</h1>
    <p>Use transition strength as a trigger for operating posture, safeguards, and deployment gates.</p>
  </header>

  <div class="card mt-4 overflow-x-auto p-4">
    <table class="w-full min-w-[880px] border-collapse text-sm">
      <thead>
        <tr class="text-left">
          <th class="border-b px-3 py-2">Transition signal</th>
          <th class="border-b px-3 py-2">Survival posture</th>
          <th class="border-b px-3 py-2">Decision gate</th>
        </tr>
      </thead>
      <tbody>
        {transitionBridgeRows.map((row, index) => (
          <tr>
            <td class={`${index < transitionBridgeRows.length - 1 ? 'border-b' : ''} px-3 py-2 font-semibold`}>{row.signal}</td>
            <td class={`${index < transitionBridgeRows.length - 1 ? 'border-b' : ''} px-3 py-2`}>{row.posture}</td>
            <td class={`${index < transitionBridgeRows.length - 1 ? 'border-b' : ''} px-3 py-2`}>{row.gate}</td>
          </tr>
        ))}
      </tbody>
    </table>
  </div>
</section>

<section id="stack-model" class="view-surface mt-4">
  <header class="view-surface-header">
    <p class="view-surface-kicker">Stack model</p>
    <h1>Integrated operating stack</h1>
    <p>Decision-grade layers for sequencing AI deployment without compromising life-critical outcomes.</p>
  </header>

  <div class="card mt-4 overflow-x-auto p-4">
    <table class="w-full min-w-[980px] border-collapse text-sm">
      <thead>
        <tr class="text-left">
          <th class="border-b px-3 py-2">Layer</th>
          <th class="border-b px-3 py-2">Decision focus</th>
          <th class="border-b px-3 py-2">Systems</th>
          <th class="border-b px-3 py-2">AI role</th>
          <th class="border-b px-3 py-2">What to monitor</th>
        </tr>
      </thead>
      <tbody>
        {stackModelRows.map((row, index) => (
          <tr>
            <td class={`${index < stackModelRows.length - 1 ? 'border-b' : ''} px-3 py-2 font-semibold`}>{row.layer}</td>
            <td class={`${index < stackModelRows.length - 1 ? 'border-b' : ''} px-3 py-2`}>{row.focus}</td>
            <td class={`${index < stackModelRows.length - 1 ? 'border-b' : ''} px-3 py-2`}>{row.systems}</td>
            <td class={`${index < stackModelRows.length - 1 ? 'border-b' : ''} px-3 py-2`}>{row.aiRole}</td>
            <td class={`${index < stackModelRows.length - 1 ? 'border-b' : ''} px-3 py-2`}>{row.monitor}</td>
          </tr>
        ))}
      </tbody>
    </table>
    <p class="mt-3 text-sm text-[var(--tone-muted)]">
      Rule of thumb: if L0-L1 indicators degrade, prioritize reliability and safeguards before increasing autonomy.
    </p>
  </div>
</section>

<section id="systems" class="view-surface mt-4">
  <header class="view-surface-header">
    <p class="view-surface-kicker">Systems map</p>
    <h1>What keeps a human alive</h1>
    <p>
      Tiered by urgency: <strong>Keep Alive</strong> then <strong>Stabilize</strong>.
    </p>
  </header>

  <div class="mt-4 grid gap-4">
    {SURVIVAL_OS.systems.map((sys) => (
      <details class="card p-4">
        <summary class="cursor-pointer list-none">
          <div class="flex items-baseline justify-between gap-4">
            <div>
              <div class="view-surface-kicker">{sys.tier}</div>
              <div class="mt-1 font-display text-lg">{sys.title}</div>
            </div>
            <div class="chip rounded-full px-3 py-1 text-xs">Open</div>
          </div>
        </summary>

        <p class="mt-4 text-[var(--tone-muted)]">{sys.whyItMatters}</p>

        <div class="mt-4 grid gap-4">
          <div>
            <div class="view-surface-kicker">Minimums</div>
            <ul class="list-disc pl-5">
              {sys.minimums.map((x) => <li>{x}</li>)}
            </ul>
          </div>

          <div>
            <div class="view-surface-kicker">Dependencies</div>
            <ul class="list-disc pl-5">
              {sys.dependencies.map((x) => <li>{x}</li>)}
            </ul>
          </div>

          <div>
            <div class="view-surface-kicker">What AI could automate</div>
            <ul class="list-disc pl-5">
              {sys.whatAiCouldAutomate.map((x) => <li>{x}</li>)}
            </ul>
          </div>

          <div>
            <div class="view-surface-kicker">Safeguards</div>
            <ul class="list-disc pl-5">
              {sys.safeguards.map((x) => <li>{x}</li>)}
            </ul>
          </div>
        </div>
      </details>
    ))}
  </div>
</section>

<section class="view-surface mt-4 lab-only">
  <header class="view-surface-header">
    <p class="view-surface-kicker">Research notes</p>
    <h1>Publication hardening</h1>
    <p>Editorial scaffolding preserved for Lab Mode review.</p>
  </header>

  <details class="mt-4 card p-4" open>
    <summary class="cursor-pointer list-none font-semibold">Fact tune-ups before publish</summary>
    <div class="mt-3 grid gap-3">
      {tuneUps.map((item) => (
        <article class="card p-4">
          <h3 class="font-semibold">{item.claim}</h3>
          <p class="mt-1 text-[var(--tone-muted)]">{item.note}</p>
          <p class="mt-2 text-sm">
            <a class="underline" href={item.source} target="_blank" rel="noreferrer">Source</a>
          </p>
        </article>
      ))}
    </div>
  </details>
</section>

<section id="sources" class="view-surface mt-4">
  <header class="view-surface-header">
    <p class="view-surface-kicker">Sources</p>
    <h1>Primary references</h1>
    <p>Use these as baseline references for the report page and infographic captions.</p>
  </header>

  <ul class="mt-4 list-disc space-y-1 pl-5 text-sm">
    {tuneUps.map((item) => (
      <li>
        <a class="underline" href={item.source} target="_blank" rel="noreferrer">{item.source}</a>
      </li>
    ))}
  </ul>
</section>
